export default {
  theme: {
    extend: {
      colors: {
        create: "var(--btn-create-bg)",
        update: "var(--btn-update-bg)",
        read: "var(--btn-read-bg)",
        delete: "var(--btn-delete-bg)",
      }
    }
  }
}
